import { ErrorMessageForm } from './error'
import { InputForm } from './input'
import { LabelForm } from './label'
import { FormRoot } from './root'

export const FormInput = {
  Root: FormRoot,
  Input: InputForm,
  Label: LabelForm,
  ErrorMessage: ErrorMessageForm
}
